import React, { FC } from 'react';
import Modal from "../Modal";

const LoginModal: FC = () => {
    return (
        <Modal>
            <button>Войти</button>
        </Modal>
    );
};

export default LoginModal;